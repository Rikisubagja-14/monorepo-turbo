export interface UserData {
    email: string;
    password: string;
    name: string;
    phoneNumber: string;
    address: string;
  }
  
export interface UserLogin {
    email: string;
    password: string;
  }
  